<Window x:Class="PptPoc.App.StatusIndicatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="StatusIndicator" Height="40" Width="40"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        Opacity="0.8">
    <Window.Resources>
        <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="StatusDot" 
                             Storyboard.TargetProperty="Opacity"
                             From="0.3" To="1.0" 
                             Duration="0:0:0.6" 
                             AutoReverse="True" />
        </Storyboard>
    </Window.Resources>
    <Grid>
        <Ellipse x:Name="StatusDot" Width="20" Height="20" Fill="Gray" Stroke="DarkGray" StrokeThickness="2" Opacity="1.0">
            <Ellipse.Effect>
                <DropShadowEffect Color="Black" BlurRadius="5" Opacity="0.5" />
            </Ellipse.Effect>
        </Ellipse>
    </Grid>
</Window>