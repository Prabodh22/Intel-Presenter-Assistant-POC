<Window x:Class="PptPoc.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PPT Helper Settings" Height="330" Width="400" WindowStartupLocation="CenterScreen" Background="#F3F3F3" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="GNAI API Token" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,5"/>
        <PasswordBox x:Name="txtApiKey" Grid.Row="1" Height="25" Padding="2" Margin="0,0,0,15"/>

        <TextBlock Text="Global Toggle Hotkey" FontWeight="Bold" Grid.Row="2" Margin="0,0,0,5"/>
        <TextBox x:Name="txtHotkey" Grid.Row="3" Height="25" Padding="2" Margin="0,0,0,15"/>

        <TextBlock Text="Local ASR Model Path (Parakeet-TDT)" FontWeight="Bold" Grid.Row="4" Margin="0,0,0,5"/>
        <Grid Grid.Row="5" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtModelPath" Grid.Column="0" Height="25" Padding="2"/>
            <Button Content="Browse..." Grid.Column="1" Width="70" Height="25" Margin="5,0,0,0" Click="btnBrowse_Click"/>
        </Grid>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Height="25" Margin="0,0,10,0" Click="btnCancel_Click"/>
            <Button Content="Save" Width="80" Height="25" Background="#0078D7" Foreground="White" Click="btnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>