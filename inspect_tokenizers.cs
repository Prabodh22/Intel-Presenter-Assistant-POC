using System; using System.Reflection; using System.Linq; class Program { static void Main() { var asm = Assembly.LoadFrom(@"C:\Users\pdn\.nuget\packages\microsoft.ml.tokenizers\1.0.0\lib\net8.0\Microsoft.ML.Tokenizers.dll"); foreach (var t in asm.GetTypes().Where(t => t.Name == "Tokenizer")) { Console.WriteLine(t.FullName); foreach (var m in t.GetMethods()) { Console.WriteLine(m.Name + " " + string.Join(", ", m.GetParameters().Select(p => p.ParameterType.Name))); } } } }
